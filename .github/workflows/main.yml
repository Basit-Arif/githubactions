name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete ".git*/*"
      env:
        AWS_S3_BUCKET: "githubactiondeploy456"
        AWS_ACCESS_KEY_ID: "AKIAQOJZ3BSWWEV7536A"
        AWS_SECRET_ACCESS_KEY: "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"
        
